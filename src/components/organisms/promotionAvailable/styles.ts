import {StyleSheet} from 'react-native';

export const styleButtonsRelatedToPromo = StyleSheet.create({
  parentView: {
    justifyContent: 'space-between',
    flexDirection: 'row',
    padding: 10,
  },
});

export const stylePromotionAvailable = StyleSheet.create({
  view: {
    backgroundColor: '#e0dcdc',
    margin: 15,
  },
});

export const stylePromoInfo = StyleSheet.create({
  view: {
    alignItems: 'center',
  },
});

export const stylePromoImage = StyleSheet.create({
  img: {
    width: '100%',
    height: 150,
  },
});
