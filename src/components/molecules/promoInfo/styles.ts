import {StyleSheet} from 'react-native';

export const styleTextInformationAboutPromo = StyleSheet.create({
  text: {
    fontSize: 12,
    color: 'gray',
    fontWeight: '600',
    padding: 10,
  },
});

export const styleTitleNamePromo = StyleSheet.create({
  text: {
    fontSize: 24,
  },
});
